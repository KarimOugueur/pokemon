import { EvolutionFilterPipe } from './evolution-filter.pipe';

describe('EvolutionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EvolutionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
